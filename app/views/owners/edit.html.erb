<div class="padding">
<h1>オーナーマイページ</h1>
</div>

<div class="box5">
  <div class="owner">
    <p><%= image_tag current_owner.picture.to_s, :class =>"gazou" %></p>
    <p>名前：<%= @owner.name %></p>
    <p>性別：<%= @owner.gender %></p>
    <p>年齢：<%= @owner.age %></p>
    <p>最寄り駅：<%= @owner.station %></p>
    <p>自己紹介：<%= @owner.introduction %></p>
  </div>
</div>

<div class="padding">
<%= link_to "編集する", edit_owner_registration_path(id: current_owner.id) %>
</div>
